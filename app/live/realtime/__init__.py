"""Real-Time Classroom Engine (YC-033.2).

    from app.live.realtime.services import (
        join_classroom, leave_classroom,
        send_chat, chat_history, delete_chat,
        get_presence, heartbeat_ping,
        raise_hand, lower_hand, hand_queue, clear_hands,
        create_poll, vote_poll, poll_results, close_poll_service,
        make_announcement, pin_announcement, get_announcements,
        timeline, classroom_state,
    )
"""
