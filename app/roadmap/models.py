"""Roadmap models.

Intentionally empty — YC-006.1 is architecture only ("No database. No
models."). The roadmap content models (tracks, stages, prerequisites,
per-user progress) arrive in a later YC-006.x task, inheriting from
``app.models.BaseModel`` and applied via Flask-Migrate.
"""
